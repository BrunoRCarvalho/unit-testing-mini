const returnTwo = () => 2;

const greeting = name => `Hello, ${name}.`;

const add = (num1, num2) => num1 + num2;

const subtract = (num1, num2) => num1 - num2;

const divide = (num1, num2) => num1 / num2;

const multiply = (num1, num2) => num1 * num2;

const returnEmail = email => email;

module.exports = {
  returnTwo,
  greeting,
  add,
  returnEmail,
  divide,
  multiply,
  subtract
};
